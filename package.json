{
    "name": "chris-gilardi-portfolio",
    "scripts": {
        "build:angular": "cd angular && npm run build",
        "build:react": "cd react && npm run build",
        "build": "npm run build:angular && npm run build:react",
        "serve:static": "serve docs"
    },
    "devDependencies": {
        "serve": "^14.2.0"
    }
}
